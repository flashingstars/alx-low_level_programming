#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * int: value to be checked
 *
 * Return: 0 (Success)
 */

int _abs(int)
{
	int x;

	if (x >= 0)
	{
		return (x);
	}
	return (-x);
}
